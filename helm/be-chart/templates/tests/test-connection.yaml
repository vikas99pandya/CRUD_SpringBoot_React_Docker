apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "be-chart.fullname" . }}-test-connection"
  labels:
    {{- include "be-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "be-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
